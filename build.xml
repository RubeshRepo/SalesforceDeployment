<project name="Sample usage of Salesforce Ant tasks" default="test" basedir="." xmlns:sf="antlib:com.salesforce">

    <property file="build.properties"/>
    <property environment="env"/>

    <!-- Setting default value for username, password and session id properties to empty string 
         so unset values are treated as empty. Without this, ant expressions such as ${source.username}
         will be treated literally.
    -->
    <condition property="source.username" value=""> <not> <isset property="source.username"/> </not> </condition>
    <condition property="source.password" value=""> <not> <isset property="source.password"/> </not> </condition>
    <condition property="source.sessionId" value=""> <not> <isset property="source.sessionId"/> </not> </condition>


    <condition property="target.username" value=""> <not> <isset property="target.username"/> </not> </condition>
    <condition property="target.password" value=""> <not> <isset property="target.password"/> </not> </condition>
    <condition property="target.sessionId" value=""> <not> <isset property="target.sessionId"/> </not> </condition>



    <taskdef resource="com/salesforce/antlib.xml" uri="antlib:com.salesforce">
        <classpath>
            <pathelement location="../ant-salesforce.jar" />        	
        </classpath>
    </taskdef>
	
    <!-- Test out deploy and retrieve verbs for package 'mypkg' -->
    <target name="test">
      <!-- Upload the contents of the "mypkg" package -->
      <sf:deploy username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}" maxPoll="${source.maxPoll}" deployRoot="mypkg" rollbackOnError="true"/>
      <mkdir dir="retrieveOutput"/>
      <!-- Retrieve the contents into another directory -->
      <sf:retrieve username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}" maxPoll="${source.maxPoll}" retrieveTarget="retrieveOutput" packageNames="MyPkg"/>
    </target>

    <!-- Retrieve an unpackaged set of metadata from your org -->
    <!-- The file unpackaged/package.xml lists what is to be retrieved -->
    <target name="retrieveUnpackagedSF30Org">
      <mkdir dir="retrieveUnpackagedSF30Org"/> <!-- This comment will create folder automatically with name  retrieveUnpackaged-->
      <!-- Retrieve the contents into another directory -->
      <sf:retrieve username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}" maxPoll="${source.maxPoll}" retrieveTarget="retrieveUnpackagedSF30Org" unpackaged="unpackaged/package.xml"/>
    </target>

    <!-- Retrieve all the items of a particular metadata type -->
    <target name="bulkRetrieve">
      <sf:bulkRetrieve username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}" maxPoll="${source.maxPoll}" metadataType="${source.metadataType}" retrieveTarget="retrieveUnpackaged"/>
    </target>

    <!-- Retrieve metadata for all the packages specified under packageNames -->
    <target name="retrievePkg">
      <sf:retrieve username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}" maxPoll="${source.maxPoll}" retrieveTarget="retrieveOutput" packageNames="${source.pkgName}"/>
    </target>

    <!-- Deploy the unpackaged set of metadata retrieved with retrieveUnpackaged and run tests in this organization's namespace only-->
    <target name="deployUnpackaged">
      <sf:deploy username="${target.username}" password="${target.password}" sessionId="${target.sessionId}" serverurl="${target.serverurl}" maxPoll="${target.maxPoll}" deployRoot="deployUnpackaged" rollbackOnError="true"/>
    </target>

    <!-- Deploy a zip of metadata files to the org -->
    <target name="deployZip">
      <sf:deploy username="${target.username}" password="${target.password}" sessionId="${target.sessionId}" serverurl="${target.serverurl}" maxPoll="${target.maxPoll}" zipFile="${target.zipFile}" pollWaitMillis="1000" rollbackOnError="true"/>
    </target>

    <!-- Shows deploying code & running tests for code in directory -->
    <target name="deployCode">
      <!-- Upload the contents of the "codepkg" directory, running the tests for just 1 class -->
      <sf:deploy username="${target.username}" password="${target.password}" sessionId="${target.sessionId}" serverurl="${target.serverurl}" maxPoll="${target.maxPoll}" deployRoot="codepkg" testLevel="RunSpecifiedTests" rollbackOnError="true">
      	 <runTest>SampleDeployClass</runTest> 
      </sf:deploy>
    </target>
	
	 <!-- Shows deploying code with no TestLevel sepcified -->
    <target name="deployCodeNoTestLevelSpecified">
      <sf:deploy username="${target.username}" password="${target.password}" sessionId="${target.sessionId}" serverurl="${target.serverurl}" maxPoll="${target.maxPoll}" deployRoot="codepkg" rollbackOnError="true"/>
    </target>
	
	<!-- Shows deploying code and running tests only within the org namespace -->
	<target name="deployCodeRunLocalTests">
	  <sf:deploy username="${target.username}" password="${target.password}" sessionId="${target.sessionId}" serverurl="${target.serverurl}" maxPoll="${target.maxPoll}" deployRoot="codepkg" rollbackOnError="true"  testlevel="RunLocalTests"/>
	</target>
	
    <!-- Shows removing code; only succeeds if done after deployCode -->
    <target name="undeployCode">
      <sf:deploy username="${target.username}" password="${target.password}" sessionId="${target.sessionId}" serverurl="${target.serverurl}" maxPoll="${target.maxPoll}" deployRoot="removecodepkg"/>
    </target>

    <!-- Shows retrieving code; only succeeds if done after deployCode -->
    <target name="retrieveCode">
      <!-- Retrieve the contents listed in the file codepkg/package.xml into the codepkg directory -->
      <sf:retrieve username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}" maxPoll="${source.maxPoll}" retrieveTarget="codepkg" unpackaged="codepkg/package.xml"/>
    </target>

    <!-- Shows deploying code, running all tests, and running tests (1 of which fails), and logging. -->
    <target name="deployCodeFailingTest">
      <!-- Upload the contents of the "codepkg" package, running all tests -->
      <sf:deploy username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}" maxPoll="${source.maxPoll}" deployRoot="codepkg" testLevel="RunAllTestsInOrg" rollbackOnError="true" logType="Debugonly"/>
    </target>

    <!-- Shows check only; never actually saves to the server -->
    <target name="deployCodeCheckOnly">
      <sf:deploy username="${target.username}" password="${target.password}" sessionId="${target.sessionId}" serverurl="${target.serverurl}" maxPoll="${target.maxPoll}" deployRoot="retrieveUnpackagedPowerMechForEmail" checkOnly="true"/>
    </target>
	
	<!-- Shows quick deployment of recent validation. Set the property source.recentValidationId to your recent check only deployment Id -->
	<target name="quickDeploy">
	  <sf:deployRecentValidation  username="${target.username}" password="${target.password}" sessionId="${target.sessionId}" serverurl="${target.serverurl}" maxPoll="${target.maxPoll}" recentValidationId="${target.recentValidationId}"/>
	</target>
	
	<!-- Shows cancel deployment of deploy request either pending or in progress. Set property source.requestId to Id of pending or in progress deploy request -->
	<target name="cancelDeploy">
	  <sf:cancelDeploy  username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}" maxPoll="${source.maxPoll}" requestId="${source.requestId}"/>
	</target>

	<!-- Retrieve the information of all items of a particular metadata type -->
    <target name="listMetadata">
      <sf:listMetadata username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}" metadataType="${source.metadataType}"/>
    </target>

	<!-- Retrieve the information on all supported metadata type -->
    <target name="describeMetadata">
      <sf:describeMetadata username="${source.username}" password="${source.password}" sessionId="${source.sessionId}" serverurl="${source.serverurl}"/>
    </target>
</project>
